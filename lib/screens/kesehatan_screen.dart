import 'package:flutter/material.dart';

class KesehatanScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xFFF2F4F7), // Ganti warna dengan kode hex
      body: Center(
        child: Text('Kesehatan Page', style: TextStyle(color: Colors.black)),
      ),
    );
  }
}
